## Requirements
- nodejs


## How to run the code:

```bash
# for solution
node js/code/solution.js
# for better solution 
node js/code/better.js
```


## List of the problems found:

1. function appliquerRemise did not have a closing brace

2. function appliquerRemise was call with the wrong spelling  "appliqueRemise" 

3. the for-fool condition was out of scope "i <= produits.length"

4. misspelled "if (produit[i].type === typeCible)". it is "produits" not "produit"

5. in "(1 – tauxRemise);" , "–" is not a negative sign

6. "j" is not a varialble

7. not properly formatted

